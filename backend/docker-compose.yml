# Reference local-dev infrastructure — not started/tested in this environment.
# Run with: docker compose up -d

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: learnx
      POSTGRES_PASSWORD: learnx
      POSTGRES_DB: learnx
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: changeme
      MINIO_ROOT_PASSWORD: changeme
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  clamav:
    image: clamav/clamav:stable
    ports:
      - "3310:3310"

volumes:
  postgres_data:
  minio_data:
